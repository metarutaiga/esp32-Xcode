#!/bin/bash

ARGS=""
while [[ $# > 0 ]] ; do
    case $1 in
        -dead_strip*)
        ;;
        -F*)
        ;;
        -filelist*)
        while read -r line; do
            ARGS=$ARGS" "$line
        done < $2
        shift ;;
        -no_deduplicate*)
        ;;
        *)
        ARGS=$ARGS" "$1
        ;;
    esac
    shift
done

#echo esp-clang/bin/ld.lld $ARGS
#esp-clang/bin/ld.lld $ARGS
echo esp-clang/riscv32-esp-elf/bin/ld $ARGS
esp-clang/riscv32-esp-elf/bin/ld $ARGS
exit $?
